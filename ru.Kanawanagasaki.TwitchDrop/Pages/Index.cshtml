@page
@model IndexModel

<canvas id="twitchdropcanvas"></canvas>
<script>
    const twitchChannel = "@(Model.Channel)";
    const audioVolume = @(Model.Volume);
    const renderQuality = @(Model.Quality);
    const hideCooldown = @(Model.HideCooldown);

    const images =
    {
        @Html.Raw(Model.RenderImages(Model.GetImages("/sprites")))
    };

    const animations = @Html.Raw(Model.GetAnimations());
</script>